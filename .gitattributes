# The sample workspace is demo tooling, not product source. Mark it vendored so
# GitHub Linguist keeps the repository's language stats focused on the Go code.
sample/** linguist-vendored

# The demo GIFs are generated from the sample tapes, collapse them in diffs.
docs/demo.gif linguist-generated
docs/demo-setup.gif linguist-generated
*.gif binary
